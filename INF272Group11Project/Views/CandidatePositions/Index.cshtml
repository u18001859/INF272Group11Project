@model IEnumerable<INF272Group11Project.Models.CandidatePosition>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CandidatePosition_Description)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CandidatePosition_Description)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CandidatePosition_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.CandidatePosition_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CandidatePosition_ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
